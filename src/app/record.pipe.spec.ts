/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { RecordPipe } from './record.pipe';

describe('RecordPipe', () => {
  it('create an instance', () => {
    const pipe = new RecordPipe();
    expect(pipe).toBeTruthy();
  });
});
